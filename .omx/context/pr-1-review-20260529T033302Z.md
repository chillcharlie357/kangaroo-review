# PR #1 Review Context

## Task Statement

Review and then integrate GitHub PR #1 for `Sakiyary/kangaroo-review` after the user accepted the inline interaction direction.

## Desired Outcome

- Merge PR #1's inline direction.
- Optimize the interaction so the knowledge page shows related question titles first, then expands the full exam-question content in place.
- Keep the full question content available both in the knowledge page and in the papers page.
- Synchronize checklist state between inline knowledge-page questions and the papers page.
- Deploy only after local verification passes.

## Known Facts / Evidence

- GitHub PR #1 title: `Expand related exam questions inline on knowledge page`.
- PR state from GitHub API on 2026-05-29: open, unmerged, head `c48dcd6031224b565d2a5391a3d89af8739fd51e`, base `3eb4530a45caf22a3bced997dc663c9c6ffc1e1c`.
- `git ls-remote origin refs/pull/1/head refs/heads/master` confirmed the same head/base refs.
- Fresh `git fetch origin pull/1/head` loaded the PR into `FETCH_HEAD`.
- `git diff master...FETCH_HEAD -- site/app.js tools/smoke-site.mjs | rg ...` showed:
  - removed `pendingQuestionId`;
  - removed `jumpToQuestion`;
  - replaced `href="#papers" data-action="jump-question"` with `href="javascript:void(0)" data-action="toggle-inline-question"`;
  - changed the smoke expectation from `#papers` to `#knowledge`.
- `git diff --check master...FETCH_HEAD` passed, so the issue is behavioral/product-contract drift rather than whitespace.
- User later accepted the inline direction but clarified the final UX: show the question title, then click to expand the complete question content in place.
- The PR was merged locally, then optimized on top.

## Constraints

- Do not deploy until smoke tests and visual checks pass.
- Do not revert unrelated local/user work.
- The newer user requirement supersedes the prior jump-to-`#papers` contract for knowledge-page related questions.

## Unknowns / Open Questions

- Whether to keep a secondary "open in papers" shortcut later. It was not added in this iteration because the user asked for title -> click -> full inline content.

## Likely Codebase Touchpoints

- `site/app.js`: related question rendering, inline expansion, checklist sync, metrics.
- `tools/smoke-site.mjs`: regression contract for inline expansion and checklist sync.
- `site/styles.css`: related-question title buttons, expanded body, mobile single-column override.

## Review Verdict

Merge PR #1's direction but not as-is. The accepted implementation keeps inline title click-to-expand, tracks one `question_view` per expansion, sorts related questions by priority, syncs checklist state across duplicate question controls, and fixes the mobile expanded-content layout.
